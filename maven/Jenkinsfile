pipeline {
  agent any

  stages {
   stage('build') {
      steps {
          sh 'echo "heello" > saneeep '
           }
}
     stage('install') {
      steps {
          sh 'mvn install'
            }
}

  stage('docker step') {
      steps {
          sh 'sudo docker pull centos'
           }

}
}
}
